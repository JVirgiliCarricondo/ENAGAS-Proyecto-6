# Dependencias del prototipo — Grupo 6 (Reto 6, Enagás)
# Versiones fijadas al entorno validado (pip freeze, 7-jul-2026).
# En Windows, instalar el bloque geoespacial con conda (canal conda-forge) suele dar menos problemas que pip.

# --- Geoespacial (núcleo) ---
rasterio==1.5.0
geopandas==1.1.3
shapely==2.1.2
pyproj==3.7.2
numpy==2.5.0
pandas==3.0.3
scipy==1.18.0    # convolución del vecindario anular del TPI (superficie/tpi.py)

# --- Configuración ---
PyYAML==6.0.3    # lectura de escenario.yaml y perfiles.yaml (ingesta, superficie, trazados)

# --- Descarga de datos ---
requests==2.34.2 # Overpass, WFS IGN, ArcGIS REST IGME, OGC API Features (SNCZI)

# --- Visualización / UI ---
folium==0.20.0
streamlit==1.58.0
streamlit-folium==0.27.2
matplotlib==3.11.0  # figuras del informe PDF (src/app/informe.py)
reportlab==5.0.0    # informe PDF de resultados (comparativa + rutas + metricas)

# --- Calidad / pruebas ---
pytest
ruff

# Opcional (no requerido): richdem — acelera la pendiente Horn en superficie/tpi.py;
# sin él se usa la implementación numpy equivalente.
